namespace Pit.Javascript

open Pit

[<JsIgnore(IgnoreNamespace=true)>]
module Math =

    let abs (x:float) = 0.

    let acos (x:float) = 0.

    let asin (x:float) = 0.

    let atan (x:float) = 0.

    let atan2 (x:float) = 0.

    let ceil (x:float) = 0.

    let cos (x:float) = 0.

    let exp (x:float) = 0.

    let floor (x:float) = 0.

    let log (x:float) = 0.

    [<JsIgnore(IgnoreTuple=true)>]
    let pow (x:float) (y:float) = 0.

    let round (x:float) = 0.

    let random() = 0.

    let sin (x:float) = 0.

    let sqrt (x:float) = 0.

    let tan (x:float) = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let PI = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let E = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let LN2 = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let LN10 = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let LOG2E = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let LOG10E = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let SQR1_2 = 0.

    [<JsIgnore(IgnoreGetSet=true)>]
    let SQRT2 = 0.